# Godot 4+ specific ignores (uid_cache.bin allowed so CI can resolve UIDs when exporting)
.godot/*
!.godot/uid_cache.bin
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Editor-specific (machine-local paths)
.vscode/

# Build artifacts (most should not be committed, but EXE is needed for Game Jam)
*.pck
*.wasm
*.js
*.html
*.manifest.json
*.service.worker.js
*.worklet.js
*.offline.html
*.console.exe
